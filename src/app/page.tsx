import Header from "@/components/Header";

const Home = () => {
  return (
    <main className="relative w-full h-screen">
      <Header />
      {/* Will replace with Map */}
      <div className="bg-red-600 h-[65%] md:h-[75%]"></div>
    </main>
  );
};

export default Home;
